# JMT Dashboard — 2026-04-11

## Built / changed

### Sidebar → bottom tab bar redesign (all screen sizes)
- **Removed desktop sidebar entirely** — no more `lg:flex` sidebar column
- **Added compact top bar** (`<header>`) with logo, "JAI MUAY THAI" branding, notification bell, user name
- **Bottom tab bar on ALL screen sizes** — same nav whether mobile, iPad, or desktop. Grid columns scale to link count + "More" button
- **Profile sheet overlay** — "More" tab opens a bottom sheet with profile links, extra nav (Trial Settings, Sunday Prep, Leave, Earning), and sign-out
- **Layout updated** — removed `lg:ml-64` sidebar offset, added `pt-14` for top bar clearance, consistent `pb-24` for bottom nav
- **Notification bell positioning fixed** — reverted the `lg:fixed` hack back to simple `absolute right-0 top-full mt-2` since the bell is now in the top bar (no more sidebar overflow-hidden clipping)

### Coach dashboard redesign (Steve Jobs style) — from earlier in session
- Compact header with first name + "This week Xh" badge
- 4 horizontal stat pills replacing 2 large cards
- NEXT/NOW badges with pulse animation
- Colour-coded left borders (blue=class, orange=kids, purple=teens, green=PT)
- Tomorrow section collapsed by default with peek summary
- Week breakdown expandable

### Files changed this session
- `sidebar.tsx` — full rewrite (sidebar → top bar + bottom tab bar)
- `layout.tsx` — removed sidebar offset, adjusted padding
- `notification-bell.tsx` — reverted desktop positioning hack
- `coach-dashboard.tsx` — full redesign (carried from earlier session)

## What broke
- **Notification bell on desktop/iPad** — sidebar `overflow-hidden` was clipping the dropdown. First tried `lg:fixed` positioning (didn't work). Solved by removing the sidebar entirely and putting the bell in a top bar.
- **Cloudflare tunnel kept dying** — had to restart multiple times during the session. Port mismatch (old tunnel on 3848, new server on 3001) caught and fixed.

## Decisions made
- Isaac chose to redesign the entire sidebar rather than patch the notification overflow issue — "still the same, how about redesign the desktop and ipad view? move all the tabs"
- Bottom tab bar pattern chosen over sidebar for ALL screen sizes — consistent mobile-first UX

## What's pending
- **Isaac reviewing preview** — tunnel at `stylish-algebra-robbie-yeast.trycloudflare.com`, awaiting approval before deploy
- **Uncommitted changes (4 days old)** — leave-tab changes from 2026-04-08 still uncommitted. Now sidebar/coach-dashboard changes layered on top. Git/prod drift is significant.
  - Leave-tab: admin submit-on-behalf, admin cancel, full-time filter, AM briefing opt-out, Mode B rule
  - New: sidebar redesign, coach dashboard redesign, notification bell fix
- **Scheduled /eod agent** — Isaac asked about it; trigger list returned empty. No scheduled triggers exist on his account. Offered to recreate.
- Calendly webhook integration (parked)
- Off-in-lieu credit tracking
- Mobile app enrollment — D-U-N-S 599388227, Step 3 decision pending

## Proposals — skills

### `/jmt-sidebar`
Pattern: Isaac asks for nav/layout changes across the dashboard. The sidebar-to-tab-bar migration touched sidebar.tsx, layout.tsx, and notification-bell.tsx with interdependencies (overflow, z-index, padding offsets). A skill could encode the current nav structure (top bar + bottom tabs + profile sheet) so future changes don't re-derive the architecture.

**Verdict: not yet.** This was a one-time redesign. Only worth a skill if Isaac wants to iterate on the nav pattern again.

## Proposals — memory

### project: JMT sidebar redesigned to bottom tab bar
The JMT dashboard sidebar was removed and replaced with a top bar + bottom tab bar on all screen sizes (2026-04-11). This is the new navigation pattern. Notification bell lives in the top bar header. Profile/settings accessed via "More" bottom sheet. Not yet deployed — awaiting Isaac's preview approval.

### project: WhatsApp product brainstorm
Isaac asked "what else can we use WhatsApp to do?" (2026-04-10). Discussed 6 product ideas: booking assistant, expense tracker, lead follow-up CRM, invoice reminders, team check-in, vehicle reminders. All follow the Atom architecture pattern. Isaac hasn't picked favourites yet — waiting for his response.
