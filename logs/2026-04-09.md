# JMT Dashboard — 2026-04-09

Short morning session. One fix commit shipped before 8am, then quiet for the rest of the day.

## Built / changed

### 1. Half-day leave rule applied to AM briefing cron
Commit `724ee52` (08:03 SGT).

Coaches on approved **half-day** leave were getting the full day's schedule in their 10pm daily briefing Telegram message instead of just their evening (6:30pm+) classes. Half-day at JMT = off before 6:30pm, still teaching in the evening — see `/jmt-gotchas`.

Fix:
- In the cron route, drop morning classes and PT sessions when the coach is on half-day leave that day
- Prepend a "half day — evening only" header to the briefing message so coaches know why the list looks short
- Renamed the cron path `/api/cron/daily-schedule` → `/api/cron/jmt-am-briefing` for clearer identification in Vercel's cron dashboard
- `vercel.json` updated to match

Files touched: `src/app/api/cron/jmt-am-briefing/route.ts` (renamed from `daily-schedule`), `vercel.json`. 13 insertions, 1 deletion.

## What broke
Nothing today.

## Decisions made
- **Cron rename** — Vercel's cron dashboard lists crons by path; generic names like `daily-schedule` are hard to scan when future JMT crons are added. `jmt-am-briefing` is explicit about what it is (JMT AM briefing push).

## What's pending (carryover, nothing new added today)
- **Yesterday's leave-tab work is still uncommitted in the working tree** — `CLAUDE.md`, `src/app/(dashboard)/leave/page.tsx`, `src/app/actions/leave.ts`, `src/components/leave-page-client.tsx`. These changes were deployed to prod yesterday (see `logs/2026-04-08.md`) but never made it into a git commit. Git and prod are out of sync. Should flag to Isaac tomorrow and get these committed before any further leave-tab work, or the next edit will produce a confusing diff.
- Calendly webhook integration (parked; Isaac resumes when ready)
- Off-in-lieu credit tracking
- Mobile app enrollment — D-U-N-S obtained 2026-04-08 (599388227), Step 3 decision still awaiting

## Preview / deploy state
- `724ee52` pushed to `main` and auto-deployed by Vercel this morning (no Cloudflare Tunnel review for this one — Isaac shipped directly, which is a Mode B deviation; flagging here for awareness but not re-raising since the change is already live and working)
