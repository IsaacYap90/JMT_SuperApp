# JMT Dashboard — 2026-04-12

## Built / changed

### AM briefing — trials section added
- AM briefing cron (`jmt-am-briefing/route.ts`) now fetches today's trial bookings and includes them in each coach's morning message
- Admins see all trials; coaches see only trials for classes they're assigned to
- Half-day leave filter applied: trials before 18:30 hidden for coaches on half-day leave
- Isaac removed from `SKIP_AM_BRIEFING_USER_IDS` — he's back on the JMT brief after shelving Atom/Ion as primary

### Trial management UI improvements
- `trial-management-client.tsx`: added `notes` and `source` fields to Booking interface
- Notes now displayed inline with a 📝 icon on each booking card
- Upcoming bookings sorted by date + time_slot ascending; past bookings sorted descending

### Capacitor / mobile app scaffolding
- Added Capacitor dependencies to `package.json`: `@capacitor/core`, `@capacitor/cli`, `@capacitor/android`, `@capacitor/ios`, biometric auth, secure storage
- `capacitor.config.ts` created — appId `io.ionicx.jmtdb`, appName "JMT Dashboard", webDir `www`
- `android/` and `ios/` platform folders generated
- Mobile app plan documented in `docs/2026-04-08-mobile-app-plan.md`

### Calendly webhook integration (in progress)
- `src/app/api/webhooks/calendly/` route added
- `src/lib/calendly.ts` helper created
- `scripts/register-calendly-webhook.mjs` and `scripts/backfill-calendly-bookings.mjs` scripts added
- `supabase/add_calendly_fields_to_trial_bookings.sql` migration file created
- Middleware updated to skip auth for `/api/webhooks` paths

## What broke
- Nothing reported broken today

## What's pending
- **All changes still uncommitted** — significant drift from deployed production (carries forward from 2026-04-08 leave changes, 2026-04-11 sidebar/dashboard redesign, plus today's trial/Capacitor/Calendly work)
- **Calendly webhook integration** — route exists but unclear if fully wired up and tested
- **Mobile app** — Capacitor scaffolded, android/ios folders generated, but not built or tested on device
- **D-U-N-S 599388227** — Apple Developer enrollment still pending (Step 3 decision)
- **Sidebar redesign from 2026-04-11** — still awaiting Isaac's approval to deploy

## Decisions made
- Isaac back on JMT AM briefing (Ion/Atom shelved as primary)
- Capacitor chosen for mobile app wrapper (appId: `io.ionicx.jmtdb`)

---

## Proposals — skills

None today — no repeated pattern emerged that isn't already covered by existing skills.

## Proposals — CLAUDE.md

None — no new permanent rules surfaced.

## Proposals — memory

### project: Atom/Ion shelved as primary assistant
Isaac removed himself from the AM briefing skip list because Atom/Ion (the ionicx-chatbot that merged JMT + Google Calendar into one brief) has been shelved as the primary assistant. He's back on the standard JMT AM briefing. (Observed from the diff comment in `jmt-am-briefing/route.ts`.)

### project: Capacitor mobile app in progress
JMT Dashboard mobile app scaffolded with Capacitor (appId `io.ionicx.jmtdb`). Android and iOS platform folders generated. Biometric auth and secure storage packages added. Apple Developer enrollment pending (D-U-N-S 599388227). Not yet built or tested on device.

### project: Calendly webhook integration started
Webhook route at `/api/webhooks/calendly/`, helper lib at `src/lib/calendly.ts`, registration and backfill scripts in `scripts/`. SQL migration for Calendly fields on `trial_bookings`. Middleware updated to skip auth for webhook routes. Status: scaffolded but not confirmed end-to-end.
