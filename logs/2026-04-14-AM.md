# JMT Dashboard — 2026-04-14 AM Log

## Session summary (rewritten manually from the 12am auto-sweep)

The auto-sweep only captured git log. Real work done in this session isn't committed yet — rewriting this log so a future Claude session can pick up where we left off.

## Big ticket: Meta Lead Ads → JMT CRM (NEW, uncommitted)

Built a full mini-CRM for Jeremy to handle Meta (IG/FB) lead ads. All files are in the working tree on `main`, uncommitted.

### Files added
- `src/app/api/meta/lead-webhook/route.ts` — GET (Meta webhook verification) + POST (receives leadgen events, fetches lead from Graph API v21, inserts into `leads` table, alerts Jeremy via Telegram with wa.me link)
- `src/app/actions/leads.ts` — `getLeads`, `updateLeadStatus`, `updateLeadNotes` server actions. `Lead` and `LeadStatus` types exported from here.
- `src/app/(dashboard)/leads/page.tsx` — admin-gated server component, fetches all leads via admin client
- `src/components/leads-page-client.tsx` — stats grid, search, status filter tabs, expandable `LeadCard` with green WhatsApp button (wa.me/…), phone tel:/email mailto:, status buttons, notes textarea
- `src/components/sidebar.tsx` — MODIFIED: added `{ href: "/leads", label: "Leads", icon: "megaphone" }` to `adminMainLinks` + new `megaphone` case in `IconComponent`

### Hardcoded in webhook
`const JEREMY_USER_ID = "2ee6ecaf-f68e-4a0a-a249-0fe7ce019db8";` — Jeremy receives all alerts. Isaac explicitly said not to alert himself.

### Supabase
`leads` table created with columns: id, name, phone, email, interest, source, meta_form_id, meta_lead_id (unique), status (new|contacted|converted|lost), notes, assigned_to (fk users), created_at, updated_at. RLS policies: `admins_full_access_leads`, `service_role_leads`, `coaches_read_leads`.

### Pending before this goes live
1. Isaac gets Meta Business Suite login from Jeremy (tomorrow).
2. Set Vercel prod env vars:
   - `META_LEAD_VERIFY_TOKEN` (any string we pick — echoed back during handshake)
   - `META_PAGE_ACCESS_TOKEN` (from Meta — long-lived page token with leads_retrieval scope)
3. Register webhook URL `https://<prod>/api/meta/lead-webhook` + verify token in Meta Developer portal; subscribe to `leadgen` field on the page.
4. Jeremy taps `https://t.me/Jmt_alert_bot?start=2ee6ecaf-f68e-4a0a-a249-0fe7ce019db8` to start receiving alerts.
5. Test with a real IG/FB lead ad form submission.

### Review link (tunnel)
Sent to JMT group at 22:46 SGT: `https://recently-goals-broker-coal.trycloudflare.com/leads` — dev server on port 3001 + cloudflared quick tunnel. Tunnel will be dead by the time anyone reads this; rerun `cloudflared tunnel --url http://localhost:3001` for a fresh link.

### Build status
`npm run build` passed (fixed two `prefer-const` issues in route.ts and leads-page-client.tsx). `/leads` route: 3.48 kB.

## Other fixes committed tonight
- `5562b38` — `batch_id` type mismatch for media group photo uploads (uuid → text). Silent Supabase failure was eating multi-page contract uploads.
- `99d5198` — allow coaches to upload PT contracts via Telegram (role check now includes `coach`).

## Side tasks completed
- Created `/eod` slash command at `~/.claude/commands/eod.md` — was missing globally, so `eod-sweep.sh` was failing with "Unknown skill: eod". Fresh cron at 0 0,12 * * *.
- Parsed 12 months of 2025 payslips for IRAS tax filing. Saved payslips to `~/Documents/JMT/Payslips/2025/` and tax filing PDF to `~/Documents/JMT/Tax/2025/`.
- Fixed broken `vercel-plugin@vercel` duplicate in `~/.claude/plugins/installed_plugins.json` (installPath didn't exist → Node cjs/loader:1459 error on every UserPromptSubmit). Backup at `installed_plugins.json.bak-*`. Remaining: `vercel@claude-plugins-official` + `telegram@claude-plugins-official`.

## Recent commits (last 12h)
- `5562b38` fix: batch_id type mismatch for media group photo uploads
- `99d5198` fix: allow coaches to upload PT contracts via Telegram
- `6d189b9` feat: PT contract upload via Telegram, trial briefing/reminders, merged Clients tab

## Uncommitted changes
- Modified: `src/components/sidebar.tsx` (megaphone icon + /leads link)
- Untracked: `src/app/(dashboard)/leads/`, `src/app/actions/leads.ts`, `src/app/api/meta/`, `src/components/leads-page-client.tsx`
- Also untracked docs: `docs/2026-04-08-mobile-app-plan.md`, `docs/legal/`

## What to do on resume (for next Claude session)
1. Isaac will paste Meta login / ask to wire env vars + register webhook.
2. Once reviewed, commit the leads module on a feature branch (`feat/meta-lead-ads-crm`) → merge → deploy.
3. Test end-to-end: submit a Meta lead form → webhook fires → row appears in `leads` table → Jeremy gets Telegram alert with wa.me link → Jeremy can open `/leads` in dashboard and change status.
